<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/drawerLayout"
    tools:context=".view.PatientDrawerNavigationActivity"
    android:fitsSystemWindows="true"
    tools:openDrawer="start" >

    <include
        layout="@layout/patient_nav_toolbar"/>

    <com.google.android.material.navigation.NavigationView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/nav_menu"
        app:itemBackground="@drawable/menu_item_padding"
        app:headerLayout="@layout/patient_nav_header"
        app:menu="@menu/patient_nav_drawer_menu"
        android:layout_gravity="start"/>

</androidx.drawerlayout.widget.DrawerLayout>